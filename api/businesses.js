import{db,role,fail}from'./_lib.js';export default async function(req,res){try{await role(req,['platform_admin']);if(req.method!=='POST')return res.status(405).end();const{name,city,counterName}=req.body||{};if(!name||!counterName)throw Error('Business and counter names are required.');const b=await db('businesses',{method:'POST',headers:{'Content-Type':'application/json',Prefer:'return=representation'},body:JSON.stringify({name,city})});await db('counters',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({business_id:b[0].id,name:counterName})});res.status(201).json(b[0])}catch(e){fail(res,e)}}

import{db,role,fail}from'./_lib.js';export default async function(req,res){try{await role(req,['platform_admin']);if(req.method!=='POST')return res.status(405).json({error:'Method not allowed.'});const{name,city,counterName}=req.body||{};if(!name?.trim()||!counterName?.trim())throw Error('Business and counter names are required.');const id=crypto.randomUUID();await db('businesses',{method:'POST',headers:{'Content-Type':'application/json',Prefer:'return=minimal'},body:JSON.stringify({id,name:name.trim(),city:city?.trim()||null})});await db('counters',{method:'POST',headers:{'Content-Type':'application/json',Prefer:'return=minimal'},body:JSON.stringify({business_id:id,name:counterName.trim()})});res.status(201).json({id,name:name.trim(),city:city?.trim()||null})}catch(e){fail(res,e)}}
